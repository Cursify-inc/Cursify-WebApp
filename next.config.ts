import type { NextConfig } from "next";



const nextConfig: NextConfig = {

    // Do NOT add turbopack or experimental keys here.
    // Let Next.js 16 handle root discovery automatically.
};

export default nextConfig;
